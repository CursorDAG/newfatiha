═══════════════════════════════════════════════════════════════
  🚨 КРИТИЧЕСКАЯ ПРОБЛЕМА ОБНАРУЖЕНА
═══════════════════════════════════════════════════════════════

ОШИБКА: column "status" of relation "User" does not exist

ПРОБЛЕМА: Миграция помечена как примененная, но SQL изменения
          НЕ БЫЛИ выполнены в базе данных!

РЕШЕНИЕ: Откатить пометку миграции и применить SQL вручную

──────────────────────────────────────────────────────────────
📋 ВЫПОЛНИТЕ НА СЕРВЕРЕ (скопируйте целиком):
──────────────────────────────────────────────────────────────

cd /var/www/newfatiha

# Шаг 1: Откатить пометку миграции
npx prisma migrate resolve --rolled-back 20260320000000_add_teacher_enrollment_system

# Шаг 2: Применить миграцию заново
npx prisma migrate deploy

# Шаг 3: Генерация Prisma Client
npx prisma generate

# Шаг 4: Создать администратора
sudo -u postgres psql -d fatiha -c "DELETE FROM \"User\" WHERE email = 'admin@fatiha.ru'; INSERT INTO \"User\" (id, email, password, name, role, \"emailVerified\", status, gender, \"createdAt\", \"updatedAt\") VALUES (gen_random_uuid(), 'admin@fatiha.ru', '\$2a\$10\$N9qo8uLOickgx2ZMRZoMye.IjzKrMa3s83si9GeCAos99JxHm8jqW', 'Администратор', 'ADMIN', true, 'ACTIVE', 'NOT_SPECIFIED', NOW(), NOW());"

# Шаг 5: Перезапустить
pm2 restart fatiha

echo ""
echo "✅ ГОТОВО! Откройте https://fatiha.ru"


═══════════════════════════════════════════════════════════════
  ОДНОЙ КОМАНДОЙ:
═══════════════════════════════════════════════════════════════

cd /var/www/newfatiha && npx prisma migrate resolve --rolled-back 20260320000000_add_teacher_enrollment_system && npx prisma migrate deploy && npx prisma generate && sudo -u postgres psql -d fatiha -c "DELETE FROM \"User\" WHERE email = 'admin@fatiha.ru'; INSERT INTO \"User\" (id, email, password, name, role, \"emailVerified\", status, gender, \"createdAt\", \"updatedAt\") VALUES (gen_random_uuid(), 'admin@fatiha.ru', '\$2a\$10\$N9qo8uLOickgx2ZMRZoMye.IjzKrMa3s83si9GeCAos99JxHm8jqW', 'Администратор', 'ADMIN', true, 'ACTIVE', 'NOT_SPECIFIED', NOW(), NOW());" && pm2 restart fatiha && echo "" && echo "✅ ГОТОВО!"


═══════════════════════════════════════════════════════════════
  ЧТО ПРОИЗОШЛО:
═══════════════════════════════════════════════════════════════

1. Мы пометили миграцию как примененную (--applied)
2. НО SQL изменения не были выполнены в БД
3. Поэтому колонка "status" не существует
4. Теперь откатываем пометку (--rolled-back)
5. И применяем миграцию правильно (migrate deploy)


═══════════════════════════════════════════════════════════════
