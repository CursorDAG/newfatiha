═══════════════════════════════════════════════════════════════
  ✅ СКРИПТ ИСПРАВЛЕН! ВЫПОЛНИТЕ НА СЕРВЕРЕ:
═══════════════════════════════════════════════════════════════

⚠️  ВАЖНО: Предыдущий скрипт был с ошибкой (создавал пользователя
   ДО применения миграций). Сейчас исправлено!

──────────────────────────────────────────────────────────────
📋 КОМАНДЫ ДЛЯ СЕРВЕРА (скопируйте и выполните):
──────────────────────────────────────────────────────────────

# 1. Обновить код с GitHub (получить исправленный скрипт)
cd /var/www/newfatiha
git pull origin xnjnj

# 2. Запустить исправленный скрипт
chmod +x DEPLOY_FIX_DATABASE.sh
./DEPLOY_FIX_DATABASE.sh


═══════════════════════════════════════════════════════════════
  ИЛИ ВЫПОЛНИТЕ КОМАНДЫ ВРУЧНУЮ (если скрипт не работает):
═══════════════════════════════════════════════════════════════

cd /var/www/newfatiha

# Шаг 1: Применить миграции (СНАЧАЛА!)
npx prisma migrate deploy

# Шаг 2: Генерация Prisma Client
npx prisma generate

# Шаг 3: Проверить, что поля добавлены
sudo -u postgres psql -d fatiha -c "\d \"User\"" | grep emailVerified

# Шаг 4: Создать администратора (ПОСЛЕ миграций!)
cat > /tmp/create-admin.js << 'EOF'
const { PrismaClient } = require('@prisma/client');
const bcrypt = require('bcryptjs');
const prisma = new PrismaClient();
(async () => {
  try {
    const hash = await bcrypt.hash('admin123', 10);
    await prisma.user.deleteMany({ where: { email: 'admin@fatiha.ru' } });
    const user = await prisma.user.create({
      data: {
        email: 'admin@fatiha.ru',
        password: hash,
        name: 'Администратор',
        role: 'ADMIN',
        emailVerified: true,
        status: 'ACTIVE',
        gender: 'NOT_SPECIFIED'
      }
    });
    console.log('✅ Администратор создан:', user.email);
  } catch (e) {
    console.error('❌ Ошибка:', e.message);
    process.exit(1);
  } finally {
    await prisma.$disconnect();
  }
})();
EOF

node /tmp/create-admin.js

# Шаг 5: Перезапустить приложение
pm2 restart fatiha

# Шаг 6: Проверить логи
pm2 logs fatiha --lines 50


═══════════════════════════════════════════════════════════════
  ЧТО БЫЛО ИСПРАВЛЕНО:
═══════════════════════════════════════════════════════════════

❌ БЫЛО (неправильно):
   1. Создание пользователя
   2. Применение миграций  ← ОШИБКА! Поля еще не существуют!

✅ СТАЛО (правильно):
   1. Применение миграций  ← Сначала создаем поля в БД
   2. Генерация Prisma Client
   3. Создание пользователя ← Теперь поля существуют!


═══════════════════════════════════════════════════════════════
  ОЖИДАЕМЫЙ РЕЗУЛЬТАТ:
═══════════════════════════════════════════════════════════════

✅ Миграции применены (9 из 9)
✅ Поле User.emailVerified создано
✅ Таблицы TeacherProfile, EnrollmentRequest созданы
✅ Администратор создан: admin@fatiha.ru / admin123
✅ Приложение перезапущено
✅ Авторизация работает


═══════════════════════════════════════════════════════════════
  ПРОВЕРКА ПОСЛЕ ВЫПОЛНЕНИЯ:
═══════════════════════════════════════════════════════════════

1. Откройте: https://fatiha.ru
2. Войдите: admin@fatiha.ru / admin123
3. Должна открыться админ-панель без ошибок


═══════════════════════════════════════════════════════════════
